[project]
name = "rxdjango-dashboard"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "reflex-django",
    "pillow>=11.0.0",
]

[tool.uv.sources]
reflex-django = { path = "../reflex_django/reflex-django", editable = true }